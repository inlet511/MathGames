# NumberGame 部署文档(宝塔面板 · Windows 服务器)

本项目分前后端两部分:

- **前端 `client/`**:Cocos Creator 数学游戏合集,构建为 H5 网页(web-mobile)。
- **后端 `server/`**:排行榜服务,Fastify + TypeScript + SQLite,默认端口 **4567**。

前端通过构建产物根目录的 `config.json` 在**运行时**读取后端地址,改地址无需重新构建。

---

## 零、总体架构

推荐「同源反向代理」方案,前后端挂同一个域名:

```
浏览器  →  https://你的域名/           →  前端静态文件(Cocos 构建产物)
浏览器  →  https://你的域名/api/...     →  (Nginx 反代)  →  http://127.0.0.1:4567
```

好处:**无跨域、无 HTTPS 混合内容问题**,前端 `config.json` 的 `backendUrl` 留空即可。

---

## 一、服务器环境准备(宝塔)

1. 软件商店安装 **Node.js 版本管理器**。
2. **安装 Node.js 22 LTS(务必是 22,不要用 24)**。
   > 原因见「常见问题①」:`better-sqlite3` 只对 Node 22 提供预编译二进制,用 Node 24 会被迫源码编译,失败。
3. 安装 **PM2 管理器**(守护进程 + 开机自启)。
4. 若要用同源反代方案,确保已安装 **Nginx**(宝塔默认带)。

---

## 二、部署后端 `server/`

### 2.1 上传代码

把 `server/` 传到服务器,例如 `C:\wwwroot\numbergame_backend\`。

**只传这些**:`src/`、`package.json`、`package-lock.json`、`tsconfig.json`
**不要传**:
- `node_modules/`(平台/版本可能不匹配,服务器上重装)
- `leaderboard.db` / `.db-shm` / `.db-wal`(线上数据库,别用本地测试数据覆盖)

### 2.2 安装依赖

确认服务器 Node 是 22:`node -v` 应显示 `v22.x`。然后配国内镜像(避免 `ECONNRESET`)再安装:

```bash
npm config set registry https://registry.npmmirror.com
npm config set better_sqlite3_binary_host_mirror https://registry.npmmirror.com/-/binary/better-sqlite3/
npm install
```

装完编译 TypeScript:

```bash
npm run build      # 产物输出到 dist/
```

> 若 `npm install` 仍报 `better-sqlite3` 编译错误,见「常见问题①」的兜底方案(从本地拷二进制)。

### 2.3 用 PM2 启动

编译产物入口是 `dist/server.js`。

```bash
pm2 start dist/server.js --name numbergame
pm2 save                # 保存进程列表,配合宝塔的开机自启
```

改端口(默认 4567)用环境变量,在宝塔 PM2 管理器的项目设置里加,或命令行:

```bash
set PORT=4567 && pm2 start dist/server.js --name numbergame
```

也可在宝塔 **PM2 管理器图形界面**添加:启动文件选 `dist/server.js`,Node 版本选 22,启动。

### 2.4 验证后端

浏览器访问健康检查:

```
http://服务器IP:4567/api/health   →   返回 {"ok":true} 即成功
```

> 若外网访问不通但服务器本机能通,是防火墙/安全组没放行 4567(同源反代方案则不必对外放行,见第四节)。

---

## 三、部署前端 `client/`

### 3.1 构建

Cocos Creator 里:菜单 **项目 → 构建发布**,平台选 **Web Mobile**,构建。
产物在 `client/build/web-mobile/`。

> `client/build-templates/web-mobile/config.json` 会被自动拷到产物根目录(和 `index.html` 同级)。

### 3.2 上传

把 `build/web-mobile/` 里的**全部内容**传到前端站点根目录(如 `C:\wwwroot\numbergame\`)。
在宝塔「网站」里为该目录建一个站点 + 绑定域名 + 申请 SSL(https)。

### 3.3 配置后端地址(改这里无需重新构建)

编辑站点根目录下的 `config.json`:

| backendUrl 填法 | 适用场景 |
|---|---|
| `""`(留空) | **同源反代(推荐)**,配合第四节的 Nginx 反代 |
| `"https://api.你的域名.com"` | 后端独立域名,且**必须 https** |

改完刷新页面即生效。以后换后端地址,永远只动这一个文件。

---

## 四、配置反向代理(同源方案,推荐)

在宝塔:**网站 → 前端站点 → 反向代理 → 添加反向代理**

- 代理名称:任意(如 `api`)
- 目标 URL:`http://127.0.0.1:4567`
- **代理目录:`/api`**(关键:只转发 `/api` 开头的请求,其余走前端静态文件)

保存后,前端 `config.json` 的 `backendUrl` 保持 `""`,请求会以相对路径 `/api/...`
发出,由 Nginx 转给本机后端。这样:

- 后端 4567 端口**不必对外放行**(只本机访问),更安全;
- 前后端同域名同协议,**没有跨域,也没有 https 混合内容问题**。

---

## 五、数据库与备份

- 数据库是单文件 `server/leaderboard.db`(PM2 启动后自动创建),**这就是全部排行榜数据**。
- 备份 = 拷贝该文件。建议在宝塔「计划任务」里加定时备份(每天打包一次该文件)。
- 迁移服务器:停后端 → 拷 `leaderboard.db` 到新机对应位置 → 启后端。
- `.db-shm` / `.db-wal` 是 SQLite 的 WAL 临时文件,正常运行时存在,备份主 `.db` 即可。

---

## 六、更新发布流程

**改了后端代码**:
```bash
# 上传新的 src/ → 服务器
npm run build
pm2 restart numbergame
```

**改了前端代码**:重新构建 → 覆盖上传 `build/web-mobile/` 内容(注意别覆盖掉你已配好的 `config.json`,或重新填一次)。

**只改后端地址**:直接编辑前端站点的 `config.json`,刷新页面。无需构建、无需重启。

---

## 七、常见问题

### ① `npm install` 报 better-sqlite3 编译失败 / 找不到 Visual Studio

典型报错:`gyp ERR! find VS Could not find any Visual Studio installation`。

**原因**:`better-sqlite3` 是原生模块。当服务器 Node 版本过新(如 24),官方没有对应的预编译二进制,npm 就退化为**源码编译**,而编译需要 Visual Studio C++ 工具链——服务器上没有,于是失败。日志里的 `ECONNRESET` 说明连预编译下载源(GitHub)也不通。

**方案 A(首选):把 Node 降到 22,配镜像重装**
```bash
# 宝塔 Node 版本管理器切到 Node 22 后:
cd C:\wwwroot\numbergame_backend
rmdir /s /q node_modules            # 清掉残留
npm config set registry https://registry.npmmirror.com
npm config set better_sqlite3_binary_host_mirror https://registry.npmmirror.com/-/binary/better-sqlite3/
npm install
```
Node 22 有官方预编译二进制,配了 npmmirror 镜像后能直接下载,不再走编译。

**方案 B(兜底):从本地开发机直接拷贝已编译好的二进制**
前提:本地能正常跑后端(说明本地已有编译好的 `.node`),且**本地与服务器 Node 大版本一致(都为 22)**。

1. 本地先确认 ABI 版本一致:
   ```bash
   node -p "process.versions.modules"   # 本地与服务器都应输出 127(Node 22)
   ```
2. 服务器上照常 `npm install`(即使 better-sqlite3 那步失败也没关系,其它依赖已装好),然后:
3. 把**本地**的 `server/node_modules/better-sqlite3/build/Release/better_sqlite3.node`
   覆盖到**服务器**的 `node_modules/better-sqlite3/build/Release/` 下(没有该目录就手动建)。
4. `pm2 restart numbergame` 验证。

> ABI 号不一致(如本地 22、服务器 24)时,拷过去会报 `NODE_MODULE_VERSION` 不匹配,必须先统一 Node 版本。

### ② 前端能打开但排行榜无数据 / 提交无反应

按顺序排查:
1. 浏览器 F12 → Network,看 `/api/...` 请求的状态码。
2. **404 或请求打到了错误域名** → 反代 `/api` 没配对,或 `config.json` 的 `backendUrl` 填错。
3. **被拦截 (blocked: mixed-content)** → 前端是 https,但 `backendUrl` 填了 http 地址。改用同源反代(留空)或给后端上 https。
4. **CORS 报错** → 用独立域名方案时后端未放行来源。同源反代方案不会有此问题。
5. 直接访问 `/api/health` 确认后端是否活着。

### ③ 服务器重启后后端没了

确保执行过 `pm2 save`,并在宝塔 PM2 管理器里开启了开机自启。

---

## 附:游戏标识与关卡对应

| 关卡顺序 | 场景文件 | 菜单名称 | game 标识 |
|---|---|---|---|
| 1 | 01-count | 数一数 | `count` |
| 2 | 02-split | 数的分成 | `split` |
| 3 | 03-addition | 10内加减 | `addition` |
| 4 | 04-smallmath | 20内加减 | `small` |
| 5 | 05-bigmath | 100内加减 | `big` |
